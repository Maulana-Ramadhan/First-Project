function move(pl,pos) {
  ele.pl.style.transform = `translate(${pos[0]},${pos[1]})`;
}
const elementPlayer = localStorage.get("elementPlayer") || {};
if (elementPlayer?.uid) prompt("masukkan namamu: ")
document.body.appendChild();
